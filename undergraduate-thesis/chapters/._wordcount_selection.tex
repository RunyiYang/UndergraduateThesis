    % Random represents the model trained using randomly selected negative pairs and Hardest represents the model trained using hardest negative pairs. 
    % Positive denotes the average cosine similarity between anchor points and their positive samples in the feature space. 
    % Top-K is the average cosine similarity between anchor points and their top-K hardest samples in the feature space. 